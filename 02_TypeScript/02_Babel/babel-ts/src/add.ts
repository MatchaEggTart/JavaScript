/*
 * add.ts
 */
const a: number = 2;
const b: number = 3;

function add(x: number, y: number): number {
  return x + y;
}

const result: number = add(a, b);

console.log(a + " + " + b + " = " + result);
