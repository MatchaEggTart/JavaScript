"use strict";

/*
* TypeScript add func
*/

const a = 1;
const b = 2;
function add(x, y) {
  return x + y;
}

// TODO: call add func
const result = add(a, b);

// TODO: print add result to console
console.log(a + " + " + b + ' =\t', result);